services:
  crs_sync.commands:
    class: Drupal\crs_sync\Commands\CrsSyncCommands
    arguments:
      - '@crs_sync.sync_manager'
      - '@crs_sync.migration_reset'
      - '@crs_sync.migration_field_audit'
    tags:
      - { name: drush.command }
